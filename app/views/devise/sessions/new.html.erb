
<div class="row">

	<div class ="col-sm-4">
<%= simple_form_for(resource, :as => resource_name, :url => session_path(resource_name), html: { class: "well"}) do |f| %>
		<fieldset>
		<legend> Sign In </legend>

			<%= f.input :email %>
			<%= f.input :password %>

		  <% if devise_mapping.rememberable? %>
		    <div><%= f.input :remember_me, :label => 'remember_me?', as: :boolean  %></div>

		  <% end %>






		  <div><%= f.button :submit, "Sign in" %></div>
		<% end %>
		<%= render "devise/shared/links" %>
		</fieldset>
	</div>

	<div class="col-sm-8">
		<h1> Lets Blast off with SatCom</h1>
		<h2> Signing in is easy and secure </h2>


<div style="max-width: 500px;" id="_giphy_RWi2nt6ktdkw8"></div>
<script>var _giphy = _giphy || []; _giphy.push({id: 'RWi2nt6ktdkw8',w: 316, h: 240});var g = document.createElement('script'); g.type = 'text/javascript'; g.async = true;g.src = ('https:' == document.location.protocol ? 'https://' : 'http://') + 'giphy.com/static/js/widgets/embed.js';var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(g, s);</script>
	</div>


</div>
